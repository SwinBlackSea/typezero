# TypeZero 产品需求

## 产品定位

TypeZero 是一款 macOS AI 语音输入工具。用户通过全局快捷键完成录音，结束后统一进行语音识别、纠错、分段和润色，再将最终文字写入当前光标位置。

首版优先验证 macOS 桌面端，交互参考 Typeless；后续再考虑 Android 输入法。Chrome 插件无法覆盖系统级输入场景，iOS 第三方键盘又不能直接使用麦克风，因此均不作为首版。

## 核心流程

1. 按全局快捷键开始录音；默认使用 `Control + Option + Space`，Fn 单键仅作为实验性选项。
2. 应用持续录音并显示不抢焦点的悬浮胶囊状态，不实时展示识别结果；用户可取消或结束录音。
3. 再按一次快捷键结束录音。
4. 上传完整音频并进行语音识别。
5. AI 对原始文字纠错、去除无意义重复、补充标点、分段和轻度润色；原文明确包含多项事项、步骤或待办时，整理为 `1. 2. 3.` 编号结构，普通聊天不强行列表化。
6. 将最终文字插入当前输入位置。

## MVP 范围

- macOS 菜单栏常驻应用。
- 全局快捷键开始和停止录音；默认提供可靠组合键，Fn 单键作为兼容性实验选项。
- 麦克风、输入监控和辅助功能三类权限引导：用户首次点击开始录音时，客户端请求系统原生授权；输入监控用于全局快捷键，辅助功能用于向其他应用粘贴文字。系统权限最终仍由用户确认，未授予辅助功能不阻止录音，但会导致结果仅复制到剪贴板。
- 录音中、处理中、成功和失败状态提示；录音状态使用不抢焦点的悬浮胶囊展示，提供取消和结束操作。
- 中文为主，兼容中英文混说及常见中文方言。
- 润色结果忠于原意，可直接发送或继续编辑。
- 识别失败时快速报错；润色失败时保留原始识别文字，由用户决定是否插入。
- 处理成功后始终将结果写入剪贴板，再尝试粘贴到当前光标位置；自动粘贴失败时用户仍可手动粘贴。
- 支持配置模型 API Key，密钥不得明文记录或进入日志。

## 首版不做

- 实时字幕与 WebSocket 流式识别。
- 用户系统、云端历史、团队协作、Redis、消息队列和复杂后台。
- Chrome 插件、Android 输入法和 iOS 输入法。
- Mac App Store 上架；优先通过签名、公证后的安装包分发。

## 成功标准

- 结束录音后能在可接受时间内返回文字，处理状态明确。
- 常用中文听写准确，润色不篡改原意。
- 能在浏览器、聊天软件和编辑器等常见输入框中稳定插入文字；即使粘贴失败，结果也保留在剪贴板。
- 授权输入监控后，组合键能在其他应用前台时稳定触发录音并显示悬浮状态。
- 模型供应商可替换，不影响客户端操作流程。
